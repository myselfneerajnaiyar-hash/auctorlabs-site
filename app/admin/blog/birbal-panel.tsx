"use client";
import { useState } from "react";
export type BirbalMessage={role:"user"|"birbal";text:string};type Proposal={type:string;target:string;replacement:string;summary:string}[];type Props={title:string;busy:boolean;onAsk:(message:string,history:BirbalMessage[])=>Promise<{reply:string;proposal?:Proposal;applied?:boolean}>;onApply:(proposal:Proposal)=>Promise<void>;onUndo:()=>Promise<void>};
export default function BirbalPanel({title,busy,onAsk,onApply,onUndo}:Props){const[input,setInput]=useState(""),[messages,setMessages]=useState<BirbalMessage[]>([]),[proposal,setProposal]=useState<Proposal|null>(null);async function ask(value=input){const message=value.trim();if(!message||busy)return;setInput("");setMessages(current=>[...current,{role:"user",text:message}]);try{const result=await onAsk(message,messages);setMessages(current=>[...current,{role:"birbal",text:result.reply}]);setProposal(result.proposal||null)}catch(error){setMessages(current=>[...current,{role:"birbal",text:(error as Error).message}])}}return <section className="mt-5 rounded-2xl border border-violet-400/25 bg-violet-500/[.06] p-5" aria-label="Birbal editorial assistant"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-bold tracking-[.2em] text-violet-300">BIRBAL</p><h3 className="mt-1 text-lg font-semibold">Your AI editorial assistant</h3><p className="mt-1 text-xs text-slate-400">Working on: {title}</p></div>{messages.some(message=>message.role==="birbal"&&message.text.includes("updated"))&&<button disabled={busy} onClick={()=>void onUndo()} className="rounded-lg border border-white/15 px-3 py-2 text-xs">Undo</button>}</div><div className="mt-4 flex flex-wrap gap-2">{["Fix review issues","Improve introduction","Improve SEO","Add examples","Add an image","Improve readability"].map(action=><button key={action} disabled={busy} onClick={()=>void ask(action)} className="rounded-full border border-violet-300/20 bg-black/20 px-3 py-1.5 text-xs text-violet-100 disabled:opacity-40">{action}</button>)}</div><div className="mt-4 max-h-48 space-y-2 overflow-auto">{messages.map((message,index)=><p key={index} className={`rounded-lg p-3 text-sm ${message.role==="user"?"ml-8 bg-white/10":"mr-8 bg-black/25 text-slate-200"}`}><b>{message.role==="user"?"You":"Birbal"}:</b> {message.text}</p>)}</div>{proposal&&<div className="mt-4 rounded-xl border border-amber-400/25 bg-amber-500/10 p-4 text-sm"><p className="font-semibold text-amber-200">Birbal proposes:</p><ul className="mt-2 list-disc space-y-1 pl-5 text-amber-100">{proposal.map((item,index)=><li key={index}>{item.summary}</li>)}</ul><div className="mt-3 flex gap-2"><button disabled={busy} onClick={async()=>{await onApply(proposal);setMessages(current=>[...current,{role:"birbal",text:"Birbal updated the article. The review and audit were rerun."}]);setProposal(null)}} className="rounded-lg bg-violet-400 px-3 py-2 text-xs font-bold text-black">Apply changes</button><button onClick={()=>setProposal(null)} className="rounded-lg border border-white/15 px-3 py-2 text-xs">Cancel</button></div></div>}<form className="mt-4 flex gap-2" onSubmit={event=>{event.preventDefault();void ask()}}><input value={input} onChange={event=>setInput(event.target.value)} placeholder="Ask Birbal to change anything..." className="min-w-0 flex-1 rounded-xl border border-white/10 bg-black/30 px-4 py-3 text-sm"/><button disabled={busy||!input.trim()} className="rounded-xl bg-violet-400 px-4 py-3 text-sm font-bold text-black disabled:opacity-40">Ask Birbal</button></form></section>}
