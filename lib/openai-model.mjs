export function resolveOpenAiTextModel(value=process.env.OPENAI_TEXT_MODEL){const configured=String(value||"").trim();return !configured||configured.toLowerCase()==="gpt-5.6-luna"?"gpt-5-mini":configured;}
