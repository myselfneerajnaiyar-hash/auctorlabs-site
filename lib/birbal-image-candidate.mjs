function clean(value){return String(value||"").replace(/\s+/g," ").trim();}
export function resolveBirbalImageCandidate({planned=[],placement,brief,validPlacements=[],existingIds=[]}){const target=clean(placement),explicit=clean(brief);if(!validPlacements.includes(target))throw new Error(`The requested image section does not exist: ${target}`);const plannedCandidate=planned.find(image=>image.placement===target);if(plannedCandidate)return{...plannedCandidate,source:"planner"};if(!explicit)throw new Error("Birbal did not provide a usable visual brief for this image.");const base=`birbal-${target}`,used=new Set(existingIds);let id=base,suffix=2;while(used.has(id))id=`${base}-${suffix++}`;return{id,type:"editorial",placement:target,purpose:explicit,scene:explicit,peopleContext:"A believable human editorial scene grounded in the approved article section.",composition:"Wide 3:2 premium editorial photograph with a clear focal point and natural light.",prompt:`Create a premium photorealistic editorial image. Approved visual brief: ${explicit}. Communicate the idea clearly in a believable human scene. Use a wide 3:2 composition, natural professional lighting, and no readable text, labels, logos, watermarks, fake UI, or unintended diagram.`,alt:`Editorial image illustrating ${explicit.replace(/[.!?]+$/g,"").slice(0,140)}`,status:"planned",src:"",error:"",source:"birbal-fallback"};}
